[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "acrostic-agent-watermark"
version = "0.11.1"
description = "Agent-level digital watermarking — universal Agent plugin for user ID traceability"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "AAWM Team"}]
dependencies = [
    "numpy>=1.24",
    "scipy>=1.10",
]

[project.optional-dependencies]
langchain = ["langchain>=0.3"]
litellm = ["litellm>=1.40"]
server = ["fastapi>=0.100", "uvicorn>=0.20", "pydantic>=2.0", "httpx>=0.24"]
dev = ["pytest>=7.0", "pytest-asyncio", "httpx"]
nlp = ["nltk>=3.8"]
llm = ["openai>=1.0", "anthropic>=0.20"]

[project.scripts]
aawm = "aawm.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
aawm = ["data/*.json"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
